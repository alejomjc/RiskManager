---
securityDefinitions:
  JWT:
    type: apiKey
    in: header
    name: Authorization
    description: Use the JWT token obtained from the /get-token endpoint for authentication.

tags:
  - "risks"
summary: "Get a list of risks"
description: "This route retrieves a list of risks."
responses:
  200:
    description: "List of risks retrieved successfully"
    content:
      application/json:
        schema:
          type: array
          items:
            type: object
  401:
    description: "Unauthorized"
